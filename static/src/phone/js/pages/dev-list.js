window.addEventListener('load', function () {
    setTimeout(function () {
        // 注释待续...
        (function () {
            // 功能待续...
        }());

        require('../../scss/pages/dev-list.scss');// 当前页面用到的样式
        const common = require('../common/common');// 每个页面都要用到的js(一定要放到最底部)
    }, 0);
});
